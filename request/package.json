{
  "name": "@bigoncloud/request",
  "version": "1.0.1",
  "description": "Wrapper around supertest",
  "main": "dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "yarn clean && tsc",
    "pub": "yarn build && npm version patch"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.7",
    "@types/supertest": "^2.0.10",
    "rimraf": "^3.0.2",
    "supertest": "^4.0.2",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
